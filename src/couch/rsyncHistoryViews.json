{
  "_id": "_design/history",
  "views": {
    "asymByDate": {
      "map": "function (doc) { if (doc.type === 'asymUnit') emit(doc._id, null); }"
    },
    "bioAssemblyByDate": {
      "map": "function (doc) { if (doc.type === 'bioAssembly') emit(doc._id, null); }"
    }
  }
}
